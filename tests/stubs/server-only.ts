// Test stub for the `server-only` package.
// The real module throws when imported outside a React Server Component,
// which would stop server-side modules being unit-tested at all. Aliased in
// vitest.config.ts only — the app still imports the real guard.
export {};
